import { useState } from "react";

export default function Squisher({}) {
    const [imageArchive, setimageArchive] = useState([])
    return (
        <div>
            <p>This is the thing that does the squishing </p>
        </div>
    )
}